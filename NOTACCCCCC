#include<stdio.h>
int main()
{
    int a,d,arr[100000]={0};
    scanf("%d\n", &a);
    if(a == 0)
    {
        arr[d]++;
    }
    else
    {
        for(int i = 0; i < a; i++)
        {
            scanf("%d",&d);
            arr[d]++;
        }
        for(int j = 0; j <= d; j++)
        {
            if(arr[j] == 1)
            {
                printf("%d ",j);
            }
        }
        printf("\n");
    }
    return 0;
}
